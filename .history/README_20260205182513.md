# 🐳 Browser Automation with Docker & Puppeteer

Complete setup for running headless Chrome browser automation inside Docker containers.

## 📋 Prerequisites

- **Docker Desktop** installed ([Download](https://www.docker.com/products/docker-desktop))
- **Windows**: Docker Desktop for Windows
- **Mac**: Docker Desktop for Mac
- **Linux**: Docker Engine

Verify installation:
```bash
docker --version
```

## 🚀 Quick Start

### Windows Users
```bash
run.bat
```

### Mac/Linux Users
```bash
chmod +x run.sh
./run.sh
```

### Manual Steps

**Step 1: Build Docker Image**
```bash
docker build -t browser-automation .
```

**Step 2: Run Container**
```bash
docker run --rm -v "%cd%:/app" browser-automation
```

## 📁 Project Structure

```
sample/
├── script.js           # Puppeteer automation script
├── package.json        # Node.js dependencies
├── Dockerfile          # Docker container definition
├── .dockerignore       # Files to exclude from Docker
├── run.bat             # Windows automation script
├── run.sh              # Mac/Linux automation script
└── screenshot.png      # Output from the script (generated)
```

## 🔍 What the Script Does

1. ✅ Launches headless Chrome inside the container
2. ✅ Opens https://example.com
3. ✅ Extracts page title
4. ✅ Extracts all H1 tags
5. ✅ Extracts first 5 links
6. ✅ Takes a screenshot
7. ✅ Closes browser gracefully